language: node_js
cache:
  directories:
    - ~/.npm
    - ~/.cache
addons:
  apt:
    packages:
      - xvfb
before_install:
  - "npm i -g npm@latest"
node_js:
  - "lts/*"
script:
  - "npm run lint"
  - "npm run build"
  - "npm run cypress:run --  --record --key b43d988f-5145-4a2b-9df3-ce3b1607f203"
