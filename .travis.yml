language: node_js
cache:
  directories:
    - ~/.npm
    - ~/.cache
before_install:
  - "npm i -g npm@latest"
node_js:
  - "lts/*"
script:
  - "npm run lint"
  - "npm run build"
  - "npm run cypress:run --  --record"
